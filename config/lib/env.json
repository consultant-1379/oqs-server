{
  "LDAP_URL":{
    "description": "URL and port of the LDAP server used for authentication",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  },
  "BASE_DN_LIST":{
    "description": "Colon separated list of Base DNs",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  },
  "SEARCH_FILTER": {
    "description": "LDAP filter used to find the user",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  },
  "OQS_EMAIL_ADDRESS": {
    "description": "OQS's email address is used for sender parameter.",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  },
  "DTT_EMAIL_ADDRESS": {
    "description": "DTT's email address is to give OQS access to send emails.",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  },
  "TEAM_EMAIL": {
    "description": "Team email address is used for recipient parameter.",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  },
  "DTT_EMAIL_PASSWORD": {
    "description": "DTT's email address password to allow OQS to send emails",
    "validators": [{
      "name": "length",
      "options": {"min": 1}
    }]
  }
}
